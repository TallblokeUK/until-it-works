#!/usr/bin/env python3
"""mp-agent: see `mp-agent --help`. The code lives in ../mp_agent."""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), ".."))

from mp_agent.cli import main  # noqa: E402

sys.exit(main(sys.argv[1:]))
